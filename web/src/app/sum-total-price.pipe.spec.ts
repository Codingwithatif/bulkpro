import { SumTotalPricePipe } from './sum-total-price.pipe';

describe('SumTotalPricePipe', () => {
  it('create an instance', () => {
    const pipe = new SumTotalPricePipe();
    expect(pipe).toBeTruthy();
  });
});
